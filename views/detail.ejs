
<div class="container mx-auto">
    <h1 class="text-center font-bold text-cyan-300 text-4xl p-5">Welcome to Contact Page</h1>
    <div class="flex justify-center py-16">
        <div class="w-auto h-auto rounded-lg shadow-xl bg-slate-600">
            <div class="p-3 w-full">
                <h1 class="text-cyan-300 pt-3">Nama: <span class="text-white"><%= contact.nama %></span> </h1>
                <h1 class="text-cyan-300">NoHp: <span class="text-white"><%= contact.noHp %></span> </h1>
                <h1 class="text-cyan-300">Email: <span class="text-white"><%= contact.email %></span> </h1>
                <div class="py-4">
                    <!-- <a href="/contact/delete/<%= contact.nama %>" class="py-1 px-3 bg-pink-500 rounded-lg font-normal capitalize text-slate-800" onclick="return confirm('yakin kidz?')">hapus</a> -->
                    <form method="POST" action="/contact?_method=DELETE" class="inline">
                        <input type="hidden" name="nama" value="<%= contact.nama %>">
                        <button type="submit" class="py-1 px-3 bg-pink-500 rounded-lg font-normal capitalize text-slate-800" onclick="return confirm('yakin kidz?')">hapus</button>
                      </form>

                    <a href="/contact/change/<%= contact.nama %>" class="py-1 px-3 bg-lime-300 rounded-lg font-normal capitalize text-slate-800">ubah</a>
                </div>
                <div class="py-4">
                    <a href="/contact" class="text-slate-900 bg-cyan-300 rounded-xl px-3 py-1">Kembali ke contact</a>

                </div>
            </div>
        </div>
    </div>
</div>